name: Create envfile

on: [ push ]

jobs:

  create-envfile:
 
    runs-on: ubuntu-latest

    steps:
    - name: Make envfile
        echo "${{ secrets.CONTACT_FORM }}" > .env
        echo "${{ secrets.CONTACT_SERVICE }}" > .env
        echo "${{ secrets.PUBLIC_KEY }}" > .env
